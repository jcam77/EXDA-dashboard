# NOtes from Marcus:
The data for all experiments within one set (and the settings) is stored in a folder called [experimentname].exp (it have a ending like a file but is a folder). 
I exported the data to an ASCII file which got the file ending .asd.

I have sent you both the exported and non-exported test file (with a few experiments included). The best would be to be able to work directly on the files before exporting.